🎯 **What:** The `findBrevInstanceStatus` function parses an input JSON string. If `JSON.parse` fails (e.g. invalid JSON format), it has a `catch` block that suppresses the error and returns `null`. This behavior was entirely untested.
📊 **Coverage:** Added a unit test covering the `JSON.parse` failure case inside `findBrevInstanceStatus`. It directly passes "invalid json" as the input to the parser and asserts it successfully returns `null`.
✨ **Result:** Improved test coverage for `deploy.ts` and enhanced robustness guarantees against unhandled exceptions from improperly formatted CLI outputs.

Signed-off-by: Jules <jules@code.ai>
