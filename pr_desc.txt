🎯 **What:** The `isHealthProbeOk` function was missing tests for its error handling path, specifically when `JSON.parse(body)` throws an error. Also the function was not exported. I exported the function and added a test suite.

📊 **Coverage:** Covered all scenarios for the health probe responses:
- Happy paths (`"ok"`, `{"status": "ok"}`).
- Invalid JSON strings (`"invalid json"`).
- Empty strings, `null`, and `undefined` handling.
- Valid JSON strings with status different from `"ok"`.

✨ **Result:** Improved overall coverage by explicitly hitting the `catch` block within `isHealthProbeOk`. It prevents untested refactor regressions.

Signed-off-by: Jules <jules@example.com>
