# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

name: Verify

on:
  pull_request:
  push:
    branches: [main]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22.16.0
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Fast truth and drift gates
        run: npm run verify:status-truth && npm run verify:version-drift

      - name: Threat seam verification
        run: npm run verify:threat-seams

      - name: Governance scoreboard
        run: npm run verify:governance

      - name: Release verification
        run: npm run verify:release-readiness

      - name: Typecheck CLI
        run: npm run typecheck:cli

      - name: Clean git diff check
        run: git diff --check
